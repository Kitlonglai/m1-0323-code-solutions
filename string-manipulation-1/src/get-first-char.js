/* exported getFirstChar */
/* First define a function named getFirstChar that accepts one parameter of a string.
Next write a return statement in the function code block that returns the string at array index 0.
We can treat the string like an array so getting the 0th index allows us to get the first characer of string. */
function getFirstChar(string) {
  return string[0];
}
